<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
.style_image{
  height: 100px;
  width: 200px;
}
</style>
</head>
<body>

<h2>Images Realated to Album <%= @album.name%></h2>

<table class="container-all">
  <tr>
    <th>Image</th>
    <th>Tagline</th>
    <!-- <th>Uploaded By</th> -->
    <th>Uploaded Date</th>
  </tr>
  <% @images.each do |image| %>
    <tr>
      <td><%= image_tag image.image_url , :class => "style_image"%></td>&nbsp;&nbsp;
      <td><%= image.tagline %></td>&nbsp;&nbsp;
      <!-- <td><%#= image.user.id%></td>&nbsp;&nbsp; -->
      <td><%= image.created_date%></td>&nbsp;&nbsp;
    </tr>
    <% end %>
  
</table>

</body>
</html>


<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>
